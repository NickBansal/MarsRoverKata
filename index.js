/* eslint-disable no-undef */
process.stdout.write("\nPlease enter the coordinates of the grid, followed by the 'enter' key\n\n");
process.stdin.on("keypress", (data) => {
    // const newData = data.toString().trim();
    console.log("Hello");
});